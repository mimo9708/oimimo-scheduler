# 更新日志

本文件记录面向用户的大版本更新。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/)。

## [1.4.0] - 2026-08-18

### 新增

- **分期收款与月度收入统计**：支持整单/分期两种收款方式，分期按到账日归属月份，整单按排期月统计；收入看板月度收入/年累进/待收金额均按新口径计算
- **小票打印机**：制品明细+赠品划线+附加服务主子行+倍率/折扣/定金条件行+条码+锯齿边；四预设样式；自定义背景；主图三档；样式可存为模板复用
- **价目表增强**：分类卡片网格+长条双视图；多例图(最多10张)；分类导出PNG；价格区间；编辑看板拖拽排序
- **回复模板工具**：分组侧栏+卡片展示；创建/编辑/删除/分组管理

### 优化

- **页面切换平滑转场**：侧边栏导航升级为局部刷新+View Transitions平滑过渡，消除整页白闪；编辑中离开弹出未保存确认
- **主题引擎增强**：6套预设主题；深色主题全面支持；accent对比字色自动计算
- **配色预设**：邮票式卡片展示；自定义预设保存/选用/更新/删除
- **阶段流程**：拖拽排序+自动均分进度；流程预设管理
- **来源管理增强**：自定义来源列表+颜色配置+合并功能
- **订单删除自动清理**：删除订单后自动移除不再使用的类别/阶段/来源颜色配置

### 修复

- 截稿日期剩余天数在订单详情页正确显示
- 参考报价严格限定同类匹配
- 甘特图拖拽改期后主页逾期表报数据正确


## [1.3.2] - 2026-07-28

### 修复

- **订单新类别同步修复**：订单中录入的新稿件类别/来源现在能正确同步到设置页的稿件类别管理区和着色面板（根因：`_synced_labels` 中 `sqlite3` 未导入导致查询异常被静默吞掉）

## [1.3.1] - 2026-07-28

### 修复

- **着色面板与稿件类别管理同步**：删除稿件类别后，着色面板和管理区不再丢失仍被订单使用的类别值
- **稿件类别删除保护**：仍被订单使用的类别无法删除，标签显示订单计数，删除时弹出 toast 提示
- **数据清理**：修复历史版本遗留的 cal_commission_* 幽灵配置项

## [1.3.0] - 2026-08-02

### 新增

- **截稿日期倒计时**：编辑订单时截稿日期旁显示剩余天数徽标，过期红/临近日橙/充裕绿，修改日期实时更新
- **备份位置可配置**：设置页备份卡片新增路径输入框，可指定任意目录存放备份，留空恢复默认
- **排单列表类别筛选**：列表页新增「所有类别」下拉筛选，与阶段/来源/DDL 筛选并列
- **类别合并功能**：设置页新增合并区域，可将多个类别合并为一个（同步更新订单、列表、颜色配置）
- **备份删除按钮**：每条备份记录右侧新增红色删除按钮，确认后删除
- **阶段流程拖拽排序**：设置页阶段列表支持 SortableJS 拖拽排序，自动均分进度百分比，带迷你进度条可视化

### 优化

- 配色预设改为邮票式卡片展示：顶部色彩预览条 + 名称 + 使用中徽章，悬浮 Tooltip 改为 `?` 图标触发
- 取消启动时自动备份，仅保留手动备份入口
- 收入看板移除「本月平均时薪」卡片，精简展示
- 参考报价严格限定同类匹配，不再降级使用全局样本
- 删除订单后自动清理不再使用的类别/阶段/来源颜色配置

### 修复

- 甘特图拖拽改期后主页逾期表报 `days_remaining` 缺失错误
- 设置页阶段颜色出现「已完成」「已取消」幽灵条目
- 多处查询路径遗漏 `days_remaining` 计算字段的注入
- 编辑表单截稿日期不显示剩余天数的问题

## [1.2.0] - 2026-07-29

### 新增

- **配色预设**：设置 → 着色模式新增预设管理——把当前整套配色保存为命名预设，一键选用切换；支持原地更新（调色后覆盖存回原预设，名称不变），当前生效的预设显示「使用中」标记
- **设置页 Ctrl+S 保存**：设置页任意位置按 Ctrl+S 即可保存全部（输入框内也可触发），按键可在快捷键设置中自定义
- **平台手续费率自动填充**：订单表单切换到平台来源时，自动带入设置页配置的默认费率；手改后不回弹，编辑老单保留当时快照
- **着色模式记忆**：日历着色模式（来源/阶段/DDL/收款/类别）切换后持久保存，刷新不再回落默认值

### 修复

- 创建订单时平台手续费不自动结算的问题
- 日历拖拽改期后刷新截稿日多一天的问题（此前拖拽过的订单建议自查截稿日）
- 设置页手动改色保存后回退预设色的问题
- 自定义来源/类别不出现在颜色配置和收入图表中的问题

### 优化

- 阶段配色统一管理：「着色模式 → 按阶段」改一处，日历事件、订单徽章、甘特图、看板卡片同步变色
- 收入页品类分布图与日历「按类别」配色同源，两处颜色一致
- 更新检查改为每次启动检测一次；手动检查失败时提供下载页链接兼容离线场景

## [1.1.0] - 2026-07-28

### 新增

- **自动检测更新**：启动时每 24 小时自动检测 GitHub 最新版本，发现新版以提醒形式通知（不会自动下载）；设置 → 系统 新增「关于与更新」卡片，可手动检查更新并跳转下载页
- **健康检查接口**：新增 `GET /api/v1/health`，返回运行状态与当前版本，供浏览器插件/外部工具探活

### 修复

- 编辑订单时金额重复计算导致数值膨胀的问题
- 排期容器在窄窗口下溢出的样式问题
- 回归测试发现的 9 处小问题批次修复

## [1.0.0] - 2026-07-27

首个公开发布版本。

### 功能

- **主页仪表盘**：统计卡 5 指标（进行中 / 本月收入含环比 / 逾期 / 已完成 / 累计收入，点击查看小票明细）、甘特图排期（日/周/月/年视图，拖拽改期）、本周排单表、逾期提醒
- **日历视图**：月/周视图，未排期池拖拽入历，事件拖拽移动/调时长自动保存，5 种着色模式（来源/阶段/DDL/收款/类别）独立调色，逾期红边提醒
- **排单列表**：12 列分页表，阶段/来源/DDL/关键词多维筛选，批量操作，归档管理，Markdown 导出
- **看板**：7 阶段列拖拽流转，快捷推进按钮，列统计
- **收入看板**：月度收入 / 每月预计 / 年收入累进 / 品类分布四组图表，客户消费排名
- **画廊**：作品瀑布流无限滚动，订单多图上传（粘贴/选择），Lightbox 查看
- **客户管理**：消费金额/次数自动统计，标签管理，客户详情含历史订单与作品
- **订单**：3 种表单模式（快速模态 / 右侧抽屉 / 完整页），平台/直接两种财务模式，订单模板一键建单，归档确认链
- **设置**：18 色全局主题 + 日历调色板自定义，下拉选项（阶段/来源/类别等）免改代码配置，数据导出
- **桌面体验**：启动器自动开浏览器 + 系统托盘图标，数据全本地（SQLite 单文件）
